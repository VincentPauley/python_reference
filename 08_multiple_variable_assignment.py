#!/usr/bin/python

# you can assign the same value to multiple varaibles at once like so:

a = b = c = 'unknown'

b = 'Portal Gun'

print( a ) # 'unknown'
print( b ) # 'Portal Gun'
print( c ) # 'unknown'
